    <!-- Start Content -->
    <div id="content">
      <div class="container">

        <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name), html: { id: 'login-form'}) do |f| %>
          <h1>Sign In</h1>
          <% if devise_mapping.omniauthable? %>
            <div class="social">
              <ul>
                <li> 
                  <a href="http://localhost:3000/users/auth/facebook" class="facebook">
                    Facebook
                  </a>
                </li>
                <li>
                  <a href="users/auth/google" class="google-plus">
                    Google
                  </a>
                </li>
                <li>
                  <a href="http://localhost:3000/users/auth/linkedin" class="linkedin">
                    LinkedIn
                  </a>
                </li>
              </ul>
            </div>
       
            <div class="divider">
              <span>or</span>
            </div>
          <% end %>
          <div class="input-field">
            <input type="email" name="email" required placeholder="Email" />
            <input type="password" name="password" required placeholder="Password" />
            <% if devise_mapping.rememberable? %>
              <p style="margin-top: 20px;"><%= f.check_box :remember_me %> Remember me</p>
            <% end %>
            <hr/>
            <% if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations' %>
              <%= link_to "Forgot your password?", new_password_path(resource_name) %> 
            <% end %>
              <input style="float: right;" type="submit" value="Sign In" />
          </div>
        <% end %>

        <p class="text-p">Don't have an account? <a href="#">Sign up now!</a></p>

      </div>
    </div>
    <!-- End Content -->

